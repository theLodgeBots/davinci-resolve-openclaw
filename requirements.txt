# ResolveFlow — no external dependencies required
# Uses only Python stdlib + DaVinci Resolve scripting API
# OpenAI API called via urllib (no SDK needed)
#
# Optional: ffmpeg must be installed for speech trimming
# macOS: brew install ffmpeg
# Linux: apt install ffmpeg
